# Contributing

Before opening a pull request, run:

```bash
npm run check
npm run audit:public
npm run audit:artifacts
```

Do not commit generated run artifacts, local screenshots/videos, `dist/`, `node_modules/`, secrets, private local paths, or symlinks to files outside the repository.

For visible pet or UI changes, include screenshots or a short screen recording when practical.
